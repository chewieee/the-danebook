<div class="col-md-2">
  <%= profile_photo(item.user, :xs) %>
</div>

<div class="col-md-10">
  <%= link_to item.user.name, user_path(item.user) %>
  <p>Posted on <%= item.created_at.strftime("%B-%d-%Y") %></p>
</div>